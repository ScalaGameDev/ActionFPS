language: scala
# https://docs.travis-ci.com/user/reference/overview/
sudo: false
dist: trusty
scala:
  - 2.12.4
cache:
  directories:
    - $HOME/.ivy2/cache
    - $HOME/.sbt/boot/
    - geoip-resources
    - web/geoip-resources
script:
  - make travis
jdk:
  - oraclejdk8
